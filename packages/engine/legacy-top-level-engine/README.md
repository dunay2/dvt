# engine/ (DEPRECATED)

The top-level `engine/` source tree has been consolidated into `packages/engine`.
All active implementation and documentation should be edited under `packages/engine`.

If you have historical reference material in this folder, move or open a PR to
update the source-of-truth in `packages/engine`.

See: https://github.com/dunay2/dvt/issues/76
