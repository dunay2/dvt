# adapters/ (DEPRECATED)

The top-level `adapters/` folder is deprecated. Adapter implementations and design
artifacts now live under `packages/` (for example `packages/adapter-postgres`).

See: https://github.com/dunay2/dvt/issues/76
