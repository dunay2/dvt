{
  "name": "@dvt/adapter-postgres",
  "version": "1.0.0",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "private": true,
  "license": "MIT",
  "dependencies": {
    "@dvt/contracts": "workspace:*",
    "@dvt/engine": "workspace:*",
    "@dvt/run-domain": "workspace:*",
    "pg": "^8.16.3"
  },
  "scripts": {
    "prebuild": "pnpm --filter \"@dvt/adapter-postgres^...\" build",
    "pretypecheck": "pnpm --filter \"@dvt/adapter-postgres^...\" build",
    "pretest": "pnpm --filter \"@dvt/adapter-postgres^...\" build",
    "build": "tsc -p tsconfig.json",
    "typecheck": "tsc --noEmit -p tsconfig.json",
    "test": "vitest run --passWithNoTests --config vitest.config.ts"
  },
  "devDependencies": {
    "@types/pg": "^8.15.5",
    "typescript": "5.9.3",
    "vitest": "3.2.4"
  }
}
