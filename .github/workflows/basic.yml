name: Basic CI

# Trigger on pull requests, pushes, and manual dispatch
on:
  pull_request:
    branches: [main]
  push:
    branches: [main]
  workflow_dispatch:

jobs:
  test:
    runs-on: ubuntu-latest
    timeout-minutes: 5
    steps:
      - run: echo "✅ Workflow runs"
      - run: echo "✅ All steps executed successfully"
