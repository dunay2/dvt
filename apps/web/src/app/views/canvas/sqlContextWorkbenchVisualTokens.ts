/** Owned concern: centralize layout tokens for the embedded SQL context workbench. */
export const sqlContextWorkbenchVisualTokens = Object.freeze({
  loading: 'flex h-full items-center justify-center text-sm text-(--text-muted)',
} as const);
