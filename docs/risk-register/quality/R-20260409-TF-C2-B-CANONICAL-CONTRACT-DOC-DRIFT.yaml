---
id: R-20260409-TF-C2-B-CANONICAL-CONTRACT-DOC-DRIFT
title: TF-C2-B canonical contract docs can drift from the shipped execution read surface
status: Open
date: 2026-04-09
owners:
  - packages/@dvt/contracts
  - packages/@dvt/engine
  - apps/api
  - '@dvt/web'
severity: Medium
probability: Medium
summary: >
  TF-C2-B shipped a breaking development change from flat runtime outcome fields to a nested
  `execution` object, but the canonical engine contract docs and the frontend runtime manual still
  describe the old `RunStatusSnapshot` shape.

mitigations:
  - Keep the hard-QA artifact `20260409-tf-c2-b-read-surface-hard-qa-review.md` linked from Lane C
    until canonical docs are updated.
  - Update `IWorkflowEngine.v1.md`, `IWorkflowEngine.v1.md`, and the frontend runtime contract
    manual to describe `RunStatusSnapshot.execution` explicitly.
  - Re-run `pnpm docs:sync`, `pnpm docs:workboard:generate`, and `pnpm verify:prepush` in the debt
    payoff slice.

signals:
  - reviewers cite closeout/evidence docs for current truth because canonical contract docs are
    stale.
  - new runtime slices modify code against the wrong `RunStatusSnapshot` example.
  - frontend/manual docs omit `execution` fields that the web client consumes in production code.
---
