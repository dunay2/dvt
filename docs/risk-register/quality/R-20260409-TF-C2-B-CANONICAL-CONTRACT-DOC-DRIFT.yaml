---
id: R-20260409-TF-C2-B-CANONICAL-CONTRACT-DOC-DRIFT
title: TF-C2-B canonical contract docs can drift from the shipped execution read surface
status: Closed
date: 2026-04-09
owners:
  - packages/@dvt/contracts
  - packages/@dvt/engine
  - apps/api
  - '@dvt/web'
severity: Medium
probability: Medium
summary: >
  Canonical engine/frontend runtime docs and the current-state diagram pack now describe the shipped
  nested `execution` read-surface shape used by `TF-C2-B`, so the former flat-field documentation
  drift is no longer an active risk on the current line.

mitigations:
  - Canonical engine contract docs and the frontend runtime manual now describe
    `RunStatusSnapshot.execution` explicitly.
  - The hard-QA review artifact remains linked from Lane C as closure evidence instead of as an
    active stopgap for stale canonical docs.
  - Planning and current-state surfaces were rerun so task routing no longer treats TF-C2-B doc
    drift as an open blocker.

signals:
  - canonical engine/frontend docs now show the nested `execution` example instead of the former
    flat runtime outcome fields.
  - current planning/status surfaces no longer cite TF-C2-B contract-doc drift as an open blocker.
  - reviewers can use canonical contract entrypoints rather than closeout notes for current
    read-surface truth.
---
