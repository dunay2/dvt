---
id: R-20260403-S08-4-POSTGRES-THREE-PART-MODEL
title: Postgres three-part model migration and compatibility drift
status: Open
date: 2026-04-03
owners:
  - '@dvt/adapter-postgres'
severity: Medium
probability: Medium
---
context:
  summary: >
    S08-4 introduces a three-part persisted model in Postgres while retaining a compatibility facade
    for legacy validation lifecycle callers.
  scope:
    - packages/@dvt/adapter-postgres/src/PostgresPlanStore.ts
    - packages/@dvt/adapter-postgres/test/PostgresPlanStore.test.ts

risk:
  statement: >
    Contract evolution and compatibility bridging can drift if fixture or integration data stop
    matching canonical plan-record constraints, producing CI regressions in integration smoke.
  triggers:
    - planner metadata contract changes (planId/schemaVersion/contractVersion)
    - undocumented lifecycle transition assumptions
    - backfill logic changes without integration coverage updates

mitigation:
  implemented:
    - enforce canonical parsing via contract parsers in persistence paths
    - integration coverage for Postgres plan-store lifecycle and three-part reads/writes
    - ARC-2 evidence recorded for this slice
  next_actions:
    - complete SRP decomposition from monolithic PostgresPlanStore into focused repositories/facade
    - keep adapter-postgres integration smoke mandatory in PR checks

review:
  cadence: per PR touching S08-4 persistence paths
