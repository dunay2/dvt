DVT+ Architectural Review
Role: Principal Software Architect

Date: 2026-02-22

Sources: ADRs 0003–0016, IMPLEMENTATION_ROADMAP_FROM_REVIEW.md, WorkflowEngine.ts, @dvt/ packages, v2 architecture diagram, planning docs

1. Conceptual Soundness
   The core thesis — domain-owned execution semantics over adapter-agnostic infrastructure — is architecturally correct. The insight that "Temporal's internal state is not the source of truth; the event log is" is the most important decision in the system. Any design that derives business state from a workflow engine's internal APIs ends up coupled to that engine permanently. DVT+ avoids this.

The three-tier mantra (planner / engine / state) maps cleanly to three responsibilities:

Planner: what to execute and in what order
Engine: when to execute and what the lifecycle transitions are
State: that it happened, immutably
The problem is that only one tier is real. The planner doesn't exist as implemented code. The state store is a stub. The engine is implemented and architecturally sound, but it's a bridge to infrastructure that isn't there yet.

The ADR process is a genuine strength. Twenty-six ADRs with explicit acceptance criteria, status boards, and traceability is not normal for a pre-production system. Most teams write ADRs post-incident. This one wrote them upfront and used them as implementation guides — the IMPLEMENTATION_ROADMAP_FROM_REVIEW.md demonstrates this directly, finding 7 active bugs by comparing ADR text to code. That's architecture as a tool, not decoration.

There is a concept drift problem. Some documents describe DVT+ as a "dbt orchestration platform." Others describe a "generic deterministic workflow engine." The v2 architecture diagram shows React Flow canvas, diff views, lineage views, Snowflake cost attribution, a plugin system with sandbox execution, GraphQL API, and git sync — that's not a workflow engine, it's a platform product with 3+ years of scope. The conceptual model shifts depending on which document you're reading. This is not an engine architecture flaw. It is a product scope problem that will generate architectural pressure and mis-prioritization over time.

2. Architectural Risk Map
   Risk Severity Certainty Status
   PostgresStateStore not implemented Critical Certain Open — MVP cannot persist state

No IExecutionPlanner interface High Certain Open — users produce ExecutionPlans with no contract
No pagination in listEvents High Certain Open — full scan for 1000-step dbt runs
No stuck-run detection High Certain Open — runs can be stranded in QUEUED indefinitely
Tenant isolation is WHERE-clause only High High Open — no RLS, one query bug crosses tenants
uuid generation in Temporal workflow context High High Open — must use workflow.uuid4(), not randomUUID()
ADR-to-code drift High Certain Ongoing — 6 ADRs in Proposed/Pending with partial implementation
Two-phase write gap (bootstrapRunTx) Critical Fixed Resolved this session
vm2 referenced for plugin sandbox Critical Low-but-real Flagged in roadmap — vm2 has documented escape vulnerabilities
No event retention/archival policy Medium Certain Open — event log grows without bound
runSeq contention under parallel steps Medium High Open — if implemented as MAX+1, concurrent appends contend

Calls adapter first, then bootstrapRunTx with runRef included
Emits RunCancelRequested (intent) not RunCancelled (terminal)
Uses SHA-256 idempotency keys that exclude tenantId and payload
What remains wrong:

1. getRunStatus is not a pure read model. getRunStatus calls adapter.getRunStatus() with a 30-second timeout. ADR-0015 is unambiguous: "The default read path MUST NOT call the provider." In multi-replica deployments, the in-process circuitStateByProvider map is useless — each replica holds independent circuit state. A Temporal worker restart causes 30-second latency spikes in every status query across every replica.
2. The circuit breaker pattern is wrong by design. circuitStateByProvider: Map<string, CircuitState> inside WorkflowEngine is process-local. It provides false safety in any deployment beyond a single Node.js instance. Circuit breakers must be shared state — Redis, or an external circuit breaker service. This component needs to be removed from the engine and replaced with infrastructure-level protection.

3. capabilities validation is disabled. requiresCapabilities throws if capabilities.length > 0. Any plan that declares capabilities (which is the correct thing to do) will fail at runtime before an adapter is reached. This means the capabilities field in the plan schema is currently a runtime landmine.

This is a significant gap for three reasons:

Third, retry policy in the plan schema is defined but enforced by nothing. RetryBackoffPolicy exists as a field. No component reads it and applies it during step failure handling. The roadmap correctly specifies hardcoded 3-retry policy for Phase 1, but that decision should be in a document, not implicit in code. 5. State & Metadata Layer Review

Append-only run_events as source of truth
Monotonic run_seq per runId
Snapshot as optimistic cache for O(1) reads
Outbox pattern for reliable at-least-once delivery to the event bus
bootstrapRunTx as single atomic start point (now includes provider refs)

1. No PostgresStateStore. The only persistence is whatever in-memory implementation exists for tests. DVT+ cannot be deployed. There is no Phase 1 MVP without this.

2. Snapshot write-through is not implemented (W0-7). After each appendAndEnqueueTx, the snapshot is not updated. Every getRunStatus call requires full event replay via listEvents. For a dbt run with 1000 nodes, this is 2000+ rows fetched per status query. Under production load, listEvents will be the top-1 query by cost in the database.

3. Plugin System Evaluation
   The plugin system does not exist. What exists: a box in the v2 architecture diagram, a note in the roadmap that vm2 should not be used, and references to IPluginRuntime as a planned interface.

The recommendation to eliminate vm2 is correct. vm2 has documented sandbox escape vulnerabilities. It is not safe for production execution of user-provided code regardless of how carefully it is configured. The alternatives — worker_threads with strict IPC, V8 isolates (Deno-style), or container-per-plugin — are all more complex than vm2 for a reason. Secure plugin execution is genuinely hard.

The plugin system as described in the v2 architecture (manifest, sandbox runtime, API surface, custom plugin types) is 6–12 months of engineering work post-engine-stability. The correct decision is in the roadmap: do not start the plugin system until IExecutionPlanner and IProviderAdapter are stable and in production. Building the extension surface before the product is validated is how systems end up with elaborate plugin infrastructure and no working core.

The specific concern: if plugins can invoke adapter methods or access run state, the plugin API surface must be designed alongside those interfaces. A plugin API designed before IProviderAdapter is stable will either be too narrow (forcing breaking changes later) or too broad (creating a security boundary that's impossible to enforce). 7. What Is Overbuilt?

1. Architecture diagram scope. The v2 diagram includes React Flow canvas, Snowflake cost attribution, plugin sandbox, diff view, lineage view, GraphQL API, and git sync. This is a 3-5 year product roadmap drawn as if it were current architecture. Aspirational diagrams in early-stage systems mislead planning by creating a false picture of completeness.

2. ADR volume for unbuilt components. ADR-0002 (Neo4j as knowledge graph repository) and ADR-0006 (contract tooling governance) describe infrastructure with no code in the repository. Writing ADRs for hypothetical components produces documentation debt, not decision clarity. ADRs should follow implemented decisions, not precede unimplemented ones.

3. @dvt/traceability-service package. This directory exists. The traceability infrastructure it represents (Neo4j, graph model, query surface) is completely unbuilt. Premature package definition without implementation creates dead code that accumulates maintenance surface.
4. What Is Underbuilt?

5. TemporalAdapter (real, not stub). Second critical path item. Nothing executes without it.
6. Stuck-run detection. No mechanism exists to detect or recover from runs stranded in QUEUED. In production, a Temporal worker restart will orphan queued runs silently.

7. Multi-tenancy enforcement depth. Tenant isolation is one missing WHERE clause away from a data breach. No Row Level Security in Postgres. No audit log of access patterns. No per-tenant connection isolation.

8. Recovery procedures. What happens when Postgres is unavailable mid-transaction? When Temporal's cluster is unreachable for 30 minutes? When bootstrapRunTx partially commits? No documented recovery procedure exists. This is not optional for a system that positions itself as "reliable execution for critical workflows."

9. Scalability Outlook (3-Year Horizon)

Append-only event log partitioned by tenant_id + time scales to millions of runs with correct indexing.
Snapshot write-through (W0-7 when implemented) decouples read latency from event log size.
Adapter-first pattern means adding Conductor or BullMQ is additive, not disruptive to the engine.

In-process circuit breaker. Doesn't scale past one replica. Has to be removed.

listEvents without pagination. Unbounded. For a run with 1000 steps and 2000 events, this is megabytes per status query at production call rates.
Projection recomputation without snapshots. Without snapshot write-through, every getRunStatus call triggers a full event replay. At 10 status queries per second per run across 100 concurrent runs = 1000 listEvents queries/second. With 2000 events per run, that's 2 million rows/second just for status queries.

If plugins enter production under the worker_threads model, the horizontal scaling limit is roughly 500 concurrent plugin invocations per Node.js process before thread pool saturation.

10. Architectural Scorecard
    Dimension Score Assessment
    Operational Readiness 2/10 No stuck-run detection, no pagination, no retention policy, no recovery playbook, no dashboards.
    Security Posture 4/10 IAuthorizer exists. Tenant isolation is WHERE-clause only. No RLS. vm2 risk documented not eliminated.
    Scalability Design 6/10 Correct patterns (event sourcing, adapters, outbox). runSeq contention and circuit breaker are concrete gaps.

11. Implement W0-6 (getRunStatus read-path separation) as part of PostgresStateStore work. The read path must serve from the event log only. Separate enrichRunStatus if provider enrichment is needed for specific use cases. Remove circuitStateByProvider from the engine. Put circuit breaking in infrastructure.

12. Eliminate vm2 from all planning documents and code comments now, not when the plugin system starts. The moment vm2 appears in a code file, it will be used. Specify the secure alternative — worker_threads with strict IPC message validation, or container-per-plugin — before any plugin execution code is written.

61 lines

## Project Overview

- Monorepo at `c:\dvt`, branch `feat/ddd-cqrs-structure`
- `packages/@dvt/engine` — Core WorkflowEngine (primary work area)
- `packages/@dvt/adapter-temporal` — Temporal.io adapter (stub → real)
- `packages/@dvt/cli` — CLI tools
- `packages/@dvt/traceability-service` — Traceability (not yet built)

## Critical Invariants (ADR-backed)

- adapter.startRun() called BEFORE bootstrapRunTx (ADR-0013/0014)
- bootstrapRunTx includes runRef (no two-phase write gap)
- If bootstrapRunTx fails after adapter.startRun → adapter.cancelRun() compensation
- idempotencyKey excludes tenantId and payload (ADR-0008, RunEvents v2.0.1 §3.1)
- getRunStatus MUST NOT call adapter on default path (ADR-0015) — NOT YET DONE
- capabilities validation disabled (throws if capabilities.length > 0)

## Key Types

## Testing Patterns

- Vitest for all tests; ESM-safe `__dirname` via `fileURLToPath(import.meta.url)`
- Golden path hashes in `.golden/hashes.json`; update when plan fixtures change
- Path from `packages/@dvt/engine/test/contracts/` to root: 5 levels up (`../../../../../`)

## Workflow: File Edit Requirements

- ALWAYS read a file with Read tool before Edit or Write — tool enforces this
- "File has not been read yet" error = subagent read ≠ direct Read tool

## User Preferences

- Spanish/English mixed (responds in same language used)
- Concise surgical edits preferred
- ADR traceability on all changes
  The architectural review is complete. Key findings in summary:

Score: 5.4/10 — The engine abstraction is conceptually sound and the ADR discipline is real, but the implementation gap is large: no persistent state store, no real adapter, no planner, and several active correctness bugs.
Top 3 blockers: PostgresStateStore, TemporalAdapter, and the logicalAttemptId hardcoded-to-1 bug (which silently corrupts retry state via idempotency key collisions).
Biggest overbuilding risk: The v2 architecture diagram includes 3–5 years of product scope, which distorts planning and hides the gap between current state and running software.
Most underestimated gap: @dvt/plan-verifier doesn't exist — without it, every adapter will implement SHA-256 verification independently and they will drift.
The IMPLEMENTATION_ROADMAP_FROM_REVIEW.md already has the correct wave structure. The review validates it and adds the scoring context for prioritization decisions.

1. Build PostgresStateStore before anything else. The engine is architecturally correct. It has nowhere to persist state. All correctness investments — the ADR framework, the golden path tests, the bootstrapRunTx work — are unverifiable without a real store. Nothing else ships without this.
   Task:Explore PostgresStateStore existing code

Changes made:

adapter-postgres/src/types.ts

EventType: added 'RunCancelRequested' — the engine emits this; without it the adapter stored it as an unknown type and the snapshot projector silently ignored it
WorkflowSnapshot: added cancelling: boolean — matches WorkflowSnapshot in engine's runEvents.ts; without it every snapshot read by the engine would fail structural type checks
IRunStateStore interface: removed saveProviderRef — ADR-0013/0014 mandates adapter-first; the engine no longer calls this method
adapter-postgres/src/PostgresStateStoreAdapter.ts

Added handleRunCancelRequested function: sets snap.cancelling = true
Added case 'RunCancelRequested' to applyEventToSnapshot switch
Fixed seed object in appendEventsTxWithClient: added cancelling: false — without this the initial snapshot written on bootstrap was structurally incomplete
Added run_metadata_tenant_created_idx in ensureIndexes — listRuns(tenantId) was doing a full table scan

Removed saveProviderRef from the interface — the engine removed the call in this session; the interface needed to follow
adapter-temporal/src/engine-types.ts

Removed saveProviderRef from the local IRunStateStore copy — kept in sync with the canonical interface
adapter-postgres/test/smoke.test.ts

Replaced the three old tests (which used deprecated saveRunMetadata and appendEventsTx, and were missing planId/planVersion) with 9 focused tests covering: bootstrapRunTx atomicity, RUN_ALREADY_EXISTS, idempotency, snapshot write-through (W0-7), the full RunCancelRequested → cancelling=true → RunCancelled flow (ADR-0007), outbox delivery lifecycle, dead-letter promotion, and multi-tenant listRuns isolation
12/12 passing. What changed and why:

WorkflowEngine.ts

Removed Reason
CircuitState interface Process-local circuit breaker is useless in multi-replica
circuitStateByProvider field Same — each replica held independent state
withCircuitBreaker method Deleted entirely
circuitBreaker from WorkflowEngineDeps No caller in the engine needs it now
getRunStatus is now 8 lines: resolve meta → assert tenant → read snapshot → return. No adapter call. No timeout. No circuit breaker. Latency is bounded by the state store only.

enrichRunStatus is the new method for cases where provider substatus is needed — reads the same snapshot base, then calls adapter.getRunStatus with withTimeout. It throws if the adapter fails (no silent swallow — the caller owns the retry/fallback decision, which is where circuit breaking belongs: at the HTTP handler, behind a dedicated endpoint, with a shared Redis-backed breaker).

IWorkflowEngine.v1_1_1.ts — enrichRunStatus added with ADR-0015 doc comments distinguishing the two methods.

WorkflowEngine.test.ts — the old test was asserting the swallow behavior that no longer exists. Replaced with three tests: one proving the adapter is never called on getRunStatus, one verifying enrichRunStatus merges substatus correctly, one verifying enrichRunStatus propagates adapter failures rather than hiding them.
What was done
ExecutionPlan interface (executionPlan.ts:1) — v1.0.0 → v1.1.0:

contractVersion: string — required; the adapter rejects plans with unknown versions
plannerVersion?: string — optional; semver of the planner binary
plannerGitSha?: string — optional; 40-char SHA for traceability
generatedAt?: string — optional; ISO UTC timestamp of generation
MockAdapter (MockAdapter.ts:37):

Added SUPPORTED_CONTRACT_VERSIONS = ['1.0.0']
validateMockPlanMetadata called in startRun — throws PLAN_CONTRACT_VERSION_UNKNOWN for unknown versions
Plan fixtures — all 3 updated with "contractVersion": "1.0.0"; golden hashes recomputed (hashes.json v1.1.0 → v1.2.0)

New test file (executionPlan.contract.test.ts) — 9 contract tests:

Interface shape: required fields present, optional fields are optional
contractVersion validation: accepts "1.0.0", rejects "99.0.0" with PLAN_CONTRACT_VERSION_UNKNOWN
Provenance fields: plan with all 4 new fields executes without error
