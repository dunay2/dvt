{
  "$comment": "Instance document — NOT a JSON Schema. Reflects adapter.capabilities() declarations. Update via: pnpm generate:capabilities",
  "$generated": "2026-02-23T00:00:00.000Z",
  "$generatorSource": "adapter.capabilities() declared in each IProviderAdapter implementation",
  "$schemaRef": "./capabilities.schema.json",
  "adapters": {
    "temporal": {
      "capabilities": [
        "basic-execution",
        "signal.pause.native",
        "cancel.cooperative",
        "workflow.child.workflows",
        "workflow.fan.dynamic",
        "workflow.fan.parallel",
        "query.workflow.state",
        "replay.full",
        "workflow.continue.as.new",
        "signals.rate.limit",
        "signals.concurrent",
        "history.rotation"
      ]
    }
  }
}
