1. La desconexión entre lore.md y la estructura de carpetas
En el MASTER (sección 3), la estructura de monorepo muestra docs/lore.md como un archivo único. Pero en el contenido de lore.md se habla de lore/campaign/*.md y lore/modules/*.md. Hay una incoherencia.

Solución:

En el MASTER, la estructura debería reflejar docs/lore/campaign/ y docs/lore/modules/ (si el lore tiene múltiples archivos), o

2. La ausencia de ejemplos de schemas en el MASTER
En v0.4 teníais ejemplos concretos de JSON Schema para RunStarted y Envelope. En v0.6 han desaparecido. Los ejemplos son cruciales para que los implementadores no tengan que imaginar cómo se ve un schema real.

Sugerencia: Recuperar la sección de ejemplos de v0.4 e insertarla como subsección 8.1 o 10.1.

3. La política de gaps (sección 9) menciona Tshort y Tmax pero no da valores recomendados
Entiendo que es configurable, pero un párrafo como *"Valores típicos iniciales: Tshort = 10s, Tmax = 10min"* ayuda a los equipos a empezar sin tener que inventar números.