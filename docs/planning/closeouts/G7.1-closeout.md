---
slice: G7.1
date: 2026-03-14
last_reviewed: 2026-03-14
gap: G7
author: AI (Claude Sonnet 4.6)
---

# Closeout: G7.1 — run_snapshots formalization + rebuildSnapshot API

## Changes made

| File                                                                               | Change                                                                                                                                                                                                                                     | Why                                                                                                                                                                                            |
| ---------------------------------------------------------------------------------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------ | ---------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| `packages/@dvt/adapter-postgres/migrations/004_run_snapshots_and_status_index.sql` | New migration: `snapshot_status TEXT GENERATED ALWAYS AS (snapshot->>'status') STORED` + B-tree index                                                                                                                                      | `run_snapshots` DDL only existed in runtime code; without a numbered migration it could not be managed by external migration tooling; the index makes `listRuns` status filtering index-backed |
| `packages/@dvt/contracts/src/engine/IRunStateStore.v1.ts`                          | Add `rebuildSnapshot(tenantId, runId): Promise<WorkflowSnapshot>`                                                                                                                                                                          | The public contract needed the method so `@dvt/adapter-postgres` and external consumers implement the same signature                                                                           |
| `packages/@dvt/engine/src/ports/IRunStateStore.ts`                                 | Add `rebuildSnapshot`                                                                                                                                                                                                                      | The engine-internal port needed the method so `InMemoryRunStateStore` and `InMemoryTxStore` stay interface-complete                                                                            |
| `packages/@dvt/adapter-postgres/src/PostgresStateStoreAdapter.ts`                  | Implement `rebuildSnapshot`; update `ensureRunSnapshotsTable` (generated column); add `ADD COLUMN IF NOT EXISTS snapshot_status` to `ensureCompatibilityColumns`; add index to `ensureIndexes`; update `listRuns` to use `snapshot_status` | Full rebuild implementation with tenant isolation (ADR-0031) and replay in `runSeq ASC` order (ADR-0004)                                                                                       |
| `packages/@dvt/engine/src/state/InMemoryRunStateStore.ts`                          | Implement `rebuildSnapshot`                                                                                                                                                                                                                | Required for engine unit tests and parity with the state-store interface                                                                                                                       |
| `packages/@dvt/engine/src/state/InMemoryTxStore.ts`                                | Implement `rebuildSnapshot`                                                                                                                                                                                                                | `InMemoryTxStore` implements `IRunStateStore`; the build failed until the method existed there as well                                                                                         |
| `apps/api/src/plugins/env.ts`                                                      | Add `DVT_ADMIN_ROUTES_ENABLED` (default `false`)                                                                                                                                                                                           | Explicit exposure flag for admin routes; follows the existing `DVT_READYZ_ENABLED` pattern                                                                                                     |
| `apps/api/src/entrypoints/http/adminRoutes.ts`                                     | New: `POST /admin/runs/:runId/rebuild-snapshot`                                                                                                                                                                                            | HTTP endpoint for operational snapshot repair without direct Postgres access                                                                                                                   |
| `apps/api/src/app.ts`                                                              | Wire `registerAdminRoutes` under `DVT_ADMIN_ROUTES_ENABLED`                                                                                                                                                                                | Connect the route to the server under an explicit flag                                                                                                                                         |

## Docs synced

- [x] `docs/planning/gaps/G7-AI-EXECUTION-TRACKER.md` — current pointer moved to G7.2; Slice 1 marked done; execution log entry added
- [x] `docs/planning/gaps/GAP_EXECUTION_PLANS.md` — G7 delivered section updated; remaining work narrowed to G7.2–G7.4
- [x] `docs/architecture/system-delivery-status.md` — read-model row updated; `last_reviewed` moved to 2026-03-14

## Test evidence

| Command                                     | Result         |
| ------------------------------------------- | -------------- |
| `pnpm --filter @dvt/engine build`           | PASS           |
| `pnpm --filter @dvt/adapter-postgres build` | PASS           |
| `pnpm --filter dvt-api build`               | PASS           |
| `pnpm --filter dvt-api test`                | PASS — 35/35   |
| `pnpm --filter @dvt/engine test`            | PASS — 165/165 |

## Debt introduced

None. This slice closes without stubs, placeholders, or TODO markers.

## Known residuals (not new debt)

- `InMemoryTxStore.rebuildSnapshot` still lacks a dedicated method-level test; the build forced interface completion, but direct coverage remains part of G7.4.
- `POST /admin/runs/:runId/rebuild-snapshot` now has direct HTTP handler coverage, but this slice does not include an end-to-end test against a real Postgres instance.
