# DVT VSCode workbench alignment notes

This subfolder mirrors the same alignment assessment captured in:
`docs/planning/proposals/disposable/experiments/tabs-workbench/REPO_ALIGNMENT.md`.

This document exists only to keep the nested prototype self-contained for local
review and demo packaging. The parent document is the canonical alignment note.
