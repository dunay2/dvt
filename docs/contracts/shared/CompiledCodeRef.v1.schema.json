{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$id": "https://dvt.local/contracts/shared/CompiledCodeRef.v1.schema.json",
  "$comment": "Normative shared-kernel mirror of CompiledCodeRef from packages/@dvt/contracts/src/engine/IRunStateStore.v1.ts.",
  "title": "Compiled Code Reference",
  "description": "Content-addressable reference to a compiled SQL artifact stored outside the event log.",
  "type": "object",
  "required": ["sha256", "storageUri", "sizeBytes"],
  "additionalProperties": false,
  "properties": {
    "sha256": {
      "type": "string",
      "pattern": "^[a-f0-9]{64}$",
      "description": "SHA-256 hex digest of the compiled SQL bytes."
    },
    "storageUri": {
      "type": "string",
      "minLength": 1,
      "description": "URI pointing to the compiled SQL artifact."
    },
    "sizeBytes": {
      "type": "integer",
      "minimum": 0,
      "description": "Size of the compiled SQL blob in bytes."
    },
    "encoding": {
      "type": "string",
      "const": "utf-8",
      "description": "Optional encoding marker for the compiled SQL blob."
    }
  },
  "examples": [
    {
      "sha256": "822ae07d4783158d26f5c3ad75e62c0db7900865aba39ea1724f87b584f7f489",
      "storageUri": "s3://bucket/path/file.sql",
      "sizeBytes": 8,
      "encoding": "utf-8"
    }
  ]
}
