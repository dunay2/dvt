{
  "name": "dvt",
  "version": "1.0.0",
  "description": "DVT - Workflow Engine Documentation and Code",
  "scripts": {
    "lint:md": "markdownlint-cli2 \"**/*.md\" \"#node_modules\" \"#.git\"",
    "lint:determinism": "echo 'Determinism linting not yet implemented (placeholder for workflow engine code)' && exit 0",
    "versioning-check": "echo 'Versioning check not yet implemented (placeholder for workflow engine code)' && exit 0",
    "build": "echo 'Build not yet implemented (placeholder for workflow engine code)' && exit 0",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "repository": {
    "type": "git",
    "url": "https://github.com/dunay2/dvt.git"
  },
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "markdownlint-cli2": "^0.12.1"
  }
}
